version: "3.9"

# ── Yerel geliştirme için ────────────────────────────────────────────────────
# Google Cloud Run'a deploy için: .\deploy-gcloud.ps1
# Cloud Run image formatı: europe-west3-docker.pkg.dev/PROJECT_ID/yorunge-repo/yorunge-muhafizi:latest

services:
  yorunge-muhafizi:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    # Yerel build için: yorunge-muhafizi:latest
    # GCR push için: europe-west3-docker.pkg.dev/PROJECT_ID/yorunge-repo/yorunge-muhafizi:latest
    image: yorunge-muhafizi:latest
    container_name: yorunge_muhafizi
    ports:
      - "8501:8501"
    volumes:
      # Pipeline çıktılarını (data/) ve modeli container'a bağla.
      # Bu sayede `python main.py --all` sonrası uygulama güncel veriye erişir.
      - ./data:/app/data:ro
      - ./lightgbm_risk_modeli.pkl:/app/lightgbm_risk_modeli.pkl:ro
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8501/_stcore/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # İsteğe bağlı: pipeline'ı container içinde çalıştırmak için
  pipeline:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    image: yorunge-pipeline:latest
    container_name: yorunge_pipeline
    volumes:
      - ./data:/app/data
      - ./lightgbm_risk_modeli.pkl:/app/lightgbm_risk_modeli.pkl
      - ./cop_verileri.json:/app/cop_verileri.json:ro
      - ./turk_uydulari.json:/app/turk_uydulari.json:ro
    command: ["python", "main.py", "--all"]
    profiles:
      - pipeline
